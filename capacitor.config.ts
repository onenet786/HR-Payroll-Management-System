import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.binishaq.hrsuite',
  appName: 'Bin Ishaq HR Suite',
  webDir: 'dist'
};

export default config;
